<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kairon.saros.mapper.EmbeddingMapper">

    <!-- embeddings 表 SQL 实现（Java 接口定义见 com.kairon.saros.mapper.EmbeddingMapper） -->

    <delete id="deleteEmbedding">
        DELETE FROM embeddings
        WHERE user_id = #{userId} AND source_type = 'MANUAL' AND source_id = #{sourceId}
    </delete>

    <insert id="insertEmbedding">
        INSERT INTO embeddings (user_id, source_type, source_id, chunk_content, embedding)
        VALUES (#{userId}, 'MANUAL', #{sourceId}, #{content}, #{vector})
    </insert>

    <!-- KNN：<=> 为 pgvector cosine distance，similarity = 1 - distance（与阶段二语义一致） -->
    <select id="knnSearch" resultType="com.kairon.saros.po.KnnHit">
        SELECT source_id, chunk_content, 1 - (embedding &lt;=&gt; #{vector}) AS similarity
        FROM embeddings
        WHERE user_id = #{userId} AND source_type = 'MANUAL'
        ORDER BY embedding &lt;=&gt; #{vector}
        LIMIT #{topK}
    </select>

</mapper>
